# --- secrets ---
.env
.env.*
!.env.example
noter/notion_ids.json

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# virtualenvs
venv/
.venv/
env/

# notebooks
.ipynb_checkpoints/

# --- os / editor ---
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
.idea/
*.swp
.claude/settings.local.json

# --- logs ---
*.log
deploy/*.log
log-concat.txt

# --- personal data (this repo processes a private daily journal) ---
# per-person profiles + the registry of people
datasets/profiles/
datasets/profiles.csv
# raw daily notes copied next to the code
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9].md
5-days/

# --- pipeline output (regenerated by `python -m noter.run_daily`) ---
datasets/*.csv
datasets/*.json
datasets/from-22-jun/
# keep the docs that live in datasets/ (gold.csv + corrections.csv stay local-only:
# they hold verbatim journal lines, so they are deliberately NOT versioned)
!datasets/INCONSISTENCIES.md

# exploratory notebook — its saved cell outputs contain real journal entries
sort.ipynb
